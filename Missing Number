class Solution {
    int missingNum(int arr[]) {
          int n = arr.length + 1;
        
        Arrays.sort(arr);
        
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] != i + 1) {
                return i + 1;
            }
        }
        
        return n;
    }
}
